{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认权限：内置事件监听、关窗",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
